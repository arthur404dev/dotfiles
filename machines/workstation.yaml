# Main workstation configuration
machine:
  hostname: archlinux
  description: Primary desktop with 3 monitors and NVIDIA GPU

inherits:
  - desktop

settings:
  gpu: nvidia
  monitors: 3
  terminal: kitty

features:
  - ssh
  - github
  - docker
  - gui
